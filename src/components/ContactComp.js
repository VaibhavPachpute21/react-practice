import { Component } from "react";

export default class ContactComp extends Component{
    render(){
        return(
            <div>
                Contact Component
            </div>
        );
    }
}